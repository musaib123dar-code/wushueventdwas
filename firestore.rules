rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Global tournament records: allow public spectator read access, authorized write access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
